spring:
  profiles:
    active: local
  config:
    import: optional:file:.env[.properties]
  application:
    name: backend
  jpa:
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
